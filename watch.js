import { db } from './firebase.js';import { doc,getDoc } from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';
const id=new URLSearchParams(location.search).get('id'),root=document.getElementById('watchPage');function srcFromEmbed(value){const text=String(value||'').trim(),match=text.match(/src=["']([^"']+)["']/i);return match?match[1]:text;}
if(!id){root.innerHTML='<div class="empty">Movie ID is missing.</div>';}else{try{const snap=await getDoc(doc(db,'movies',id));if(!snap.exists()){root.innerHTML='<div class="empty">Movie not found.</div>';}else{const m={id:snap.id,...snap.data()},src=srcFromEmbed(m.embedCode);document.title=`Watch ${m.title} — CineBhai`;root.innerHTML=`<a class="back-link" href="movie.html?id=${encodeURIComponent(m.id)}">← Movie details</a><div class="player"><iframe src="${src.replace(/&/g,'&amp;').replace(/"/g,'&quot;')}" title="${String(m.title||'Movie').replace(/"/g,'&quot;')} player" allowfullscreen></iframe></div><div class="watch-title"><h1>${String(m.title||'').replace(/[&<>]/g,'')}</h1><p class="detail-meta">${m.year||''} · ${m.genre||''}</p></div>`;}}catch(e){root.innerHTML='<div class="empty">Unable to load this player.</div>';console.error(e);}}
