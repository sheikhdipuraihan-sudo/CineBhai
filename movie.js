import { db } from './firebase.js';import { doc,getDoc } from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';
const id=new URLSearchParams(location.search).get('id'),root=document.getElementById('movieDetail');
function safe(v){return String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
if(!id){root.innerHTML='<div class="empty">Movie ID is missing.</div>';}else{try{const snap=await getDoc(doc(db,'movies',id));if(!snap.exists()){root.innerHTML='<div class="empty">Movie not found.</div>';}else{const m={id:snap.id,...snap.data()};document.title=`${m.title} — CineBhai`;root.innerHTML=`<a class="back-link" href="index.html">← Back to movies</a><div class="detail-grid"><div class="detail-poster"><img src="${safe(m.posterUrl)}" alt="${safe(m.title)} poster"></div><div class="detail-copy"><p class="kicker">${safe(m.genre||'MOVIE')} ${m.trending?' · TRENDING':''}</p><h1>${safe(m.title)}</h1><p class="detail-meta">${safe(m.year)} ${m.rating?` · ★ ${Number(m.rating).toFixed(1)}`:''}</p><p class="detail-description">${safe(m.description)}</p><a class="button primary" href="watch.html?id=${encodeURIComponent(m.id)}">Watch now</a></div></div>`;}}catch(e){root.innerHTML='<div class="empty">Unable to load this movie.</div>';console.error(e);}}
